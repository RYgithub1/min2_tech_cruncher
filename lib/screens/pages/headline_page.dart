import 'package:flutter/material.dart';

class HeadlinePage extends StatelessWidget {
  // const HeadlinePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(child: Text("ssssss"),),
      ),
    );
  }
}